/**
 * Defines the ListBox component.
 *
 * @file This file defines the ListBox component, meant to be a reusable and accessible list box.
 * @since 1.0.0
 */

const ListBox: React.FC = (props) => {
  return 'ListBox Coming Soon';
};

export default ListBox;
export { ListBox };
