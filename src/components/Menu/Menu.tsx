/**
 * Defines the Menu component.
 *
 * @file This file defines the Menu component, meant to be a reusable and accessible menu.
 * @since 1.0.0
 */

const Menu: React.FC = (props) => {
  return 'Menu Coming Soon';
};

export default Menu;
export { Menu };
