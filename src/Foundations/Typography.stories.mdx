import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { Heading } from '../components/Heading/Heading';
import { Icon } from '../components/Icon/Icon';
import { Paragraph } from '../components/Paragraph/Paragraph';
import {
  DescriptionItem,
  DescriptionList
} from '../components/DescriptionList/DescriptionList';

import Lead from '../components/Lead/Lead';
import Subheading from '../storybook-utilities/Subheading';

<Meta title="Foundations/Typography" />

<Unstyled>
  <Subheading>Foundations</Subheading>
  <Heading level="1" type="article">Typography</Heading>
  <Lead>Bridging Legibility and Design</Lead>
  Typography serves as a fundamental tool in establishing both information hierarchy and clarity. This guide presents each standard HTML typography element, detailing its specific use-case.

  <div class="flex items-center gap-2">
    <Heading level="2" type="article" className="">Heading</Heading>
  </div>

<div className="canvas-alt p-4 rounded mb-8">
  <Heading type="app" level={3}>Type: App <span className="text-alt font-light italic">(Default)</span></Heading>
  <table className="min-w-full mt-8">
    <thead>
      <tr>
        <th className="text-left w-24">Level</th>
        <th className="text-left">Example</th>
      </tr>
    </thead>
    <tbody>
      {Array.from({ length: 6 }, (_, i) => i + 1).map(level => (
        <tr key={`app-${level}`}>
          <td>{level}</td>
          <td><Heading type="app" level={level} className="">Heading</Heading></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

<div className="canvas-alt p-4 rounded mb-8">
  <Heading type="app" level={3}>Type: Display</Heading>
  <table className="min-w-full mt-8">
    <thead>
      <tr>
        <th>Level</th>
        <th>Example</th>
      </tr>
    </thead>
    <tbody>
      {Array.from({ length: 6 }, (_, i) => i + 1).map(level => (
        <tr key={`display-${level}`}>
          <td>{level}</td>
          <td class="max-w-full"><Heading type="display" level={level} className="">Heading</Heading></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

<div className="canvas-alt p-4 rounded mb-8">
  <Heading type="article" level={3}>Type: Article</Heading>
  <table className="min-w-full mt-8">
    <thead>
      <tr>
        <th className="text-left w-24">Level</th>
        <th className="text-left">Example</th>
      </tr>
    </thead>
    <tbody>
      {Array.from({ length: 6 }, (_, i) => i + 1).map(level => (
        <tr key={`article-${level}`}>
          <td>{level}</td>
          <td><Heading type="article" level={level} className="">Heading</Heading></td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

  <div class="flex items-center gap-2">
    <Heading type="article" level="2">Lead</Heading>
  </div>
    <div class="canvas-alt p-8">
      <Lead>The Lead text is designed to capture attention and provide a succinct summary of the content that follows. </Lead>
    </div>


  <div class="flex items-center gap-2">
    <Heading type="article" level="2">Paragraph</Heading>
  </div>
  <div class="canvas-alt p-8">
    <Paragraph>Whiskers, the tabby cat, defies nature by hiking Mount Denali. With keen instincts and nimble paws, he reaches the summit, proving he's not just a cat but a true mountaineer.</Paragraph>
  </div>

 <div class="flex items-center gap-2">
    <Heading type="article" level="2">List</Heading>
  </div>
  <div class="canvas-alt p-8">
   <Heading type="article" level="3">Unordered List</Heading>
    <ul class="list-inside list-disc">
      <li>Item 1</li>
      <li>Item 2</li>
      <li>Item 3</li>
    </ul>

    <Heading level="3" className="mt-4">Ordered List</Heading>
    <ol class="list-inside list-decimal">
      <li>First item</li>
      <li>Second item</li>
      <li>Third item</li>
    </ol>
  </div>


<div class="flex items-center gap-2">
    <Heading type="article" level="2">Link</Heading>
  </div>
  <div class="canvas-alt p-8">
   <a href="#">This is a hyperlink</a>
  </div>

  <div class="flex items-center gap-2">
    <Heading type="article" level="2">Strong & Emphasis</Heading>
  </div>
  <div class="canvas-alt p-8">
  <strong>This text is strongly emphasized.</strong>
  <em>This text is emphasized.</em>
  </div>


  <div class="flex items-center gap-2">
    <Heading type="article" level="2">Inline Text Element</Heading>
  </div>
  <div class="canvas-alt p-8">
    <Heading type="article" level="3">Small Text</Heading>
    <small>This is smaller text.</small>

    <Heading type="article" level="3">Highlight</Heading>
    <mark>This text is highlighted or marked for emphasis.</mark>

    <Heading type="article" level="3">Deleted & Inserted</Heading>
    <del>This text is deleted.</del>
     <ins>This text is inserted.</ins>


  </div>
 

  ### 


  ### Keyboard Input
  <kbd>Ctrl + S</kbd>

  ### Variable
  <var>v</var>

  ### Sample Output
  <samp>Output sample</samp>

  ### Time
  <time dateTime="2022-10-09">October 9, 2022</time>

  ### Citation
  <cite>Author Name</cite>

  ## Block Elements <a name="block-elements"></a>
  ### Blockquote
  <blockquote>
    This is a blockquote.
  </blockquote>

  ### Code
  For inline code: <code>`code`</code>
  <pre>
    This is preformatted text for blocks of code.
  </pre>

  ### Address
  <address>
    Denali Mountain<br />
    123 Elm Street<br />
    Denali Park, AK 99755
  </address>

  ### Description List
  <DescriptionList>
    <DescriptionItem term="Term 1" description="Description for Term 1" />
    <DescriptionItem term="Term 2" description="Description for Term 2" />
  </DescriptionList>


  ## Special Text Elements <a name="special-text-elements"></a>
  ### Abbreviation
  <abbr title="Hypertext Markup Language">HTML</abbr>

  ### Bold Text
  <b>Bolded text</b>

  ### Italicized Text
  <i>Italicized text</i>

  ### Defining Term
  <dfn>Term</dfn>

  ### Inline Quotation
  <q>Inline quote</q>

  ### Subscript and Superscript
  H<sub>2</sub>O and E = mc<sup>2</sup>

  ### Bidirectional Override
  <bdo dir="rtl">This text will go right-to-left.</bdo>

  ### Line Break
  Line 1<br/>Line 2

</Unstyled>